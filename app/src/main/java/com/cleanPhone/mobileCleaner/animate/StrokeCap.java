package com.cleanPhone.mobileCleaner.animate;

import android.graphics.Paint;

public enum StrokeCap {
    BUTT(Paint.Cap.BUTT),
    ROUND(Paint.Cap.ROUND),
    SQUARE(Paint.Cap.SQUARE);

    public final Paint.Cap f4785a;

    StrokeCap(Paint.Cap cap) {
        this.f4785a = cap;
    }
}
